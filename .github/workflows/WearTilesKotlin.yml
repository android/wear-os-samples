name: WearTilesKotlin

on:
  workflow_dispatch:
  push:
    branches:
      - main
    paths:
      - 'WearTilesKotlin/**'
      - '.github/workflows/WearTilesKotlin.yml'
  pull_request:
    paths:
      - 'WearTilesKotlin/**'
      - '.github/workflows/WearTilesKotlin.yml'

env:
  SAMPLE_PATH: WearTilesKotlin

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          # Required for spotless `ratchetFrom = "origin/main"` to resolve the ref.
          fetch-depth: 0

      - name: Set up JDK 17
        uses: actions/setup-java@v6
        with:
          distribution: 'zulu'
          java-version: 17

      - name: Setup Gradle
        uses: gradle/gradle-build-action@v3
        with:
          build-root-directory: ${{ env.SAMPLE_PATH }}

      - name: Build project
        working-directory: ${{ env.SAMPLE_PATH }}
        run: ./gradlew check --stacktrace

      - name: Upload build outputs (APKs)
        uses: actions/upload-artifact@v7
        with:
          name: build-outputs
          path: ${{ env.SAMPLE_PATH }}/app/build/outputs

      - name: Upload build reports
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: build-reports
          path: ${{ env.SAMPLE_PATH }}/app/build/reports
